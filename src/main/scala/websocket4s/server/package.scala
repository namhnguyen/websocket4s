package websocket4s

/**
 * On Server side, each WebSocket Connection is handled by an Actor whose path
 * will be registered on a centralized database so that actors from different
 * servers can communicate to each other.
 *
 * Created by namnguyen on 5/22/15.
 */
package object server {

}
